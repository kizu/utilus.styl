use('current_media.js')

// Returns the current func's call stack,
// without the current func ny default,
// pass `true` to have it there
utilus_current_media($self = false)
  return _current_media($self)

current_media ?= utilus_current_media
